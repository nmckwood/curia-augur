[pytest]
# `src` puts curia_core on the path; `.` lets tests import the `tests` package helpers.
pythonpath = src .
testpaths = tests
addopts =
    -q
    --cov=src/curia_core
    --cov=apps
    --cov-report=term-missing
    --cov-fail-under=70
